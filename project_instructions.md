Instalar o python
rodar o comando
pip install -r requirements.txt

Para criar o modelo e gerar as análises
    Executar o notebook

Após isso:
Para gerar o app streamlit rodar o comando 

streamlit run streamlit/app.py --server.port=8501 "--server.address=0.0.0.0"

Acessar o app localmente em: http://localhost:8501/

Para publicar o app, executar:
